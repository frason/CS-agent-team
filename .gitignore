# Secrets — NEVER commit (holds your Claude OAuth token / GH token)
.env

# Runtime state generated by the dispatcher
.dispatcher.lock.d/
logs/
state/
artifacts/

# Coordination scratch (regenerated each run; not source)
queue/
lead-inbox/
questions/

# OS cruft
.DS_Store
